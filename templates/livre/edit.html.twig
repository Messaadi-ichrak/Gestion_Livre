{% extends 'base.html.twig' %}

{% block title %}Edit Livre{% endblock %}

{% block body %}
    <h1 class="panel-heading">Modifier Livre</h1>

    {{ include('livre/_form.html.twig', {'button_label': 'Modifier'}) }}

    <div class="container">
        <div class="row" >
            <div class="col col-lg-2">
                <a href="{{ path('livre_index') }}"><span class="btn btn-primary"><i class="fa fa-backward"></i>&nbsp;Retour</span></a>

            </div>
            {{ include('livre/_delete_form.html.twig') }}
        </div>


    </div>
{% endblock %}
