{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1 class="panel-heading">Catégorie</h1>

    <table class="table table-striped table-bordered table-hover" id="dataTables-example"
    >
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ categorie.designation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <div class="container">
        <div class="row" >
            <div class="col col-lg-6">
                <a href="{{ path('categorie_index') }}"><span class="btn btn-primary"><i class="fa fa-backward"></i>&nbsp;Retour</span></a>&nbsp;

                <a href="{{ path('categorie_edit', {'id': categorie.id}) }}"><span class="btn btn-success"><i class="fa fa-pencil"></i>&nbsp;Editer</span></a>
            </div>
            {{ include('categorie/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}
