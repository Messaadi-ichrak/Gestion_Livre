
{% extends 'base.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block body %}
    <h1 class="panel-heading">Auteur</h1>

    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
        <tbody>
        <tr>
            <th>Prénom</th>
            <td>{{ auteur.prenom }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ auteur.nom }}</td>
        </tr>
        <tr>
            <th>Biographie</th>
            <td>{{ auteur.biographie }}</td>
        </tr>
        </tbody>
    </table>

    <div class="container">
        <div class="row" >
            <div class="col col-lg-6">
                <a href="{{ path('auteur_index') }}"><span class="btn btn-primary"><i class="fa fa-backward"></i>&nbsp;Retour</span></a>&nbsp;

                <a href="{{ path('auteur_edit', {'id': auteur.id}) }}"><span class="btn btn-success"><i class="fa fa-pencil"></i>&nbsp;Editer</span></a>
            </div>
            {{ include('auteur/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}
